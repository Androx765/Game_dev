extends Node

var OrgRotation = Vector3(0,0,0)		#set in onready on "res://scenes/game/cameras/camera_controller.tscn"
var OrgPosition = Vector3(0,0,0)		#set in onready on "res://scenes/game/cameras/camera_controller.tscn"

var CameraObject = null
var CameraPivot = null
# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
